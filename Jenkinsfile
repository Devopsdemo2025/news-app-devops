pipeline {
    agent { label 'java' }
    stages {
                stage('Checkout') {
                    steps {
                        echo "checkout done"
                    }
                
				}
				}
				}
