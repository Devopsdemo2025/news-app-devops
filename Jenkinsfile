pipeline {
    agent { label 'java'}
    stages {
        stage('News') {
                    steps {
                        echo "checkout done"
                    }
                
				}
				}
				}
