pipeline {
    agent { label 'java1' }
    stages {
        stage('News') {
                stage('Checkout') {
                    steps {
                        echo "checkout done"
                    }
                }
				}
				}
				}
