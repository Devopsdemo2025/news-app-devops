pipeline {
    agent { label 'java'}
    stages {
        stage('News') {
                stage('Checkout') {
                    steps {
                        echo "checkout done"
                    }
                }
				}
				}
				}
