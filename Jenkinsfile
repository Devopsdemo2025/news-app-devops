pipeline {
    agent any
    stages {
        stage('News') {
                stage('Checkout') {
                    steps {
                        echo "checkout done"
                    }
                }
				}
				}
				}
